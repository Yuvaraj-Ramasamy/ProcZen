# ProcZen
ProcZen is a Python-powered SSH automation tool that runs multi‑server process checks, validates outputs, and sends a clean HTML status report via Outlook. Includes robust logging, error handling, and extensible command mapping.
